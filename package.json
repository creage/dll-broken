{
  "name": "dll-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build:dll && npm run build:app",
    "build:dll": "webpack",
    "build:app": "webpack"
  },
  "dependencies": {
    "babel-core": "^6.23.1",
    "babel-loader": "^6.4.0",
    "babel-preset-env": "1.3.3",
    "webpack": "^2.2.1"
  }
}
